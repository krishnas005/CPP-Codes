#include <bits/stdc++.h>
using namespace std;
#define int long long

int32_t main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
    int T;
    cin >> T;
    while (T--) {
        int a, b, c;
        cin >> a >> b >> c;
        int ans = 0;
        
    }
    return 0;
}

// one tent can accommodate 3 people
// a --> introverts --> alone
// b --> extroverts --> together with 2 others (3)
// c --> universals --> can live with any option